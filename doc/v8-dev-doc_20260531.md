# QTS V8 · 综合开发文�?v1.0

> 生成: 2026-05-31 | 基于 `doc/phase1/qts_v8_okx_devplan.html` 蓝图
> 项目: C:\Users\jerry\Desktop\V8

---

## 1. 架构全景

```
┌──────────────────────────────────────────────────────────────────�?�?                     OKX BTC-USDT-SWAP 5m                         �?�?                         数据�?                                   �?└──────────────────────┬───────────────────────────────────────────�?                       �?WebSocket (trades, books5, candle5m)
                       �?┌──────────────────────────────────────────────────────────────────�?�? Rust 热路�?(core/src/*.rs �?v8_core_engine.so)                  �?�? ┌──────────────�? ┌──────────────────�? ┌───────────────────�? �?�? �?okx_ws.rs    │→�?feature_engine.rs │→�? buffer.rs         �? �?�? �?WS订阅/解析  �? �?50d特征计算 <1μs �? �?SHM环形缓冲�?    �? �?�? └──────────────�? └──────────────────�? �?DLPack零拷贝指�? �? �?�?                                          └───────────────────�? �?�? ┌────────────────────�? ┌──────────────�? ┌───────────────────�?�?�? �?exec_channels.rs   �? │order_state.rs�? �?mcts_core.rs       �?�?�? �?HMAC签名+限流+微扰 �? �?订单FSM      �? �?Tokio UCB1树搜�?  �?�?�? └────────────────────�? └──────────────�? └───────────────────�?�?└──────────────────────────────────────────────────────────────────�?                       �?PyO3 FFI (py.allow_threads)
                       �?┌──────────────────────────────────────────────────────────────────�?�? Python 冷逻辑 (策略/研究/编排)                                    �?�? ┌────────────�? ┌──────────────�? ┌───────────────────────────┐│
�? �?Alpha引擎  �? �?特征组装     �? �?门控 G1-G5                ││
�? �?obi_v2.py  �? �?feature_     �? �?hard_gating.py            ││
�? �?funding_   �? �?fusion.py    �? �?soft_gating.py (todo)     ││
�? �?rate_arb.py�? �?Assembler    �? �?                          ││
�? └────────────�? └──────────────�? └───────────────────────────┘│
�? ┌────────────�? ┌──────────────�? ┌───────────────────────────┐│
�? �?MCTS调度   �? �?校准         �? �?编排                       ││
�? �?mcts_      �? �?recalib.py   �? �?pipeline_v2.py            ││
�? �?worker.py  �? �?temperature_ �? �?main_loop.py              ││
�? �?ev_threshold�?�?scaling.py   �? �?                          ││
�? └────────────�? └──────────────�? └───────────────────────────┘│
└──────────────────────────────────────────────────────────────────�?```

### 1.1 关键数据�?
```
WS Tick �?feature_engine.rs (解析 �?50d特征)
       �?buffer.rs (SHM写入)
       �?shm_bridge.py (DLPack零拷贝读�?
       �?feature_fusion.py (组装178d)
       �?[Phase 3+] Triton AlphaCast (gRPC推断)
       �?alphacast_recalib.py (二次校准)
       �?mcts_worker.py (MCTS规划/EV替身)
       �?pipeline_v2.py (全链路串�?
       �?hard_gating.py (G1-G5门控)
       �?risk_manager.py (Kelly仓位)
       �?exec_channels.rs (HMAC签名 �?OKX REST)
       �?order_state.rs (FSM跟踪)
       �?pnl_aggregator.py (P&L结算)
```

### 1.2 技术栈

| �?| 技�?| 版本 |
|---|------|------|
| 热路�?| Rust (PyO3 0.21) | 1.96.0 |
| AI推断 | PyTorch + Triton gRPC | 2.3.x + 2.7.x |
| 数据 | Redis + TimescaleDB | 7.x + PG16 |
| 标签 | Polars + Parquet | latest |
| 协议 | Protobuf (prost/grpcio) | schemas/*.proto |
| 包管�?| Poetry/maturin + uv | abi3-py310 |

---

## 2. 项目结构索引

```
V8/
├── core/                       # Rust 热路�?[编译产物: .so]
�?  ├── Cargo.toml              # Rust 依赖 (PyO3, tokio, prost, ring, reqwest)
�?  ├── build.rs                # Prost 自动生成
�?  └── src/
�?      ├── lib.rs              # PyO3 模块入口 (注册5个类)
�?      ├── buffer.rs           # ShmBridge (mmap2 + DLPack)
�?      ├── feature_engine.rs   # 50维特征引�?�?      ├── exec_channels.rs    # OKX REST 通道 (HMAC签名+限流)
�?      ├── order_state.rs      # 订单FSM (编译期安�?
�?      ├── mcts_core.rs        # Tokio MCTS 树搜�?�?      ├── okx_ws.rs           # OKX WS 接入
�?      └── proto_types.rs      # Prost 生成入口
�?├── schemas/                    # Protobuf 契约 [跨语言唯一数据源]
�?  ├── market_snapshot.proto   # 市场快照
�?  ├── unified_order.proto     # 统一订单
�?  ├── alpha_signal.proto      # Alpha 信号
�?  └── alphacast_output.proto  # AlphaCast 输出
�?├── alpha/crypto/               # Alpha 信号引擎
�?  ├── obi_v2.py               # OBI/OFI 订单簿因�?�?  └── funding_rate_arb.py     # 资金费率套利
�?├── features/                   # 特征工程
�?  ├── feature_fusion.py       # 注意力门控融�?(50d+12d+16d+100d�?78d)
�?  └── cross_section.py        # 时序百分位门 (Polars)
�?├── gating/hard_gating.py       # G1-G5 硬门�?(流动�?Regime/时间)
├── labeling/                   # �?新增
�?  └── counterfactual_labeler.py # CFL 反事实标签工�?�?├── calibration/                # �?新增
�?  ├── alphacast_online_update.py # 在线微调 (冻结body, 微调head)
�?  ├── temperature_scaling.py  # Temperature Scaling 在线管理
�?  └── confidence_calibrator.py # 置信度校准器
�?├── models/
�?  ├── alphacast/              # AlphaCast Transformer
�?  �?  ├── alphacast_model.py  # 模型定义 (6L8H)
�?  �?  ├── serve_alphacast.py  # Triton gRPC 客户�?�?  �?  └── alphacast_recalib.py # �?新增: 二次校准
�?  ├── resnet/
�?  �?  ├── resnet_encoder.py   # Conv1D ResNet (128d)
�?  �?  └── serve_resnet.py     # ONNX 本地推断
�?  └── mcts/                   # �?新增
�?      ├── mcts_config.py      # MCTS 参数配置
�?      ├── mcts_worker.py      # MCTS 调度�?(native/fallback)
�?      └── ev_threshold.py     # EV 阈值替�?�?├── harness/                    # 编排�?�?  ├── pipeline_v1.py          # 基础 pipeline (trace/span)
�?  ├── pipeline_v2.py          # �?新增: 全链�?v2 (多Alpha+融合+校准+MCTS)
�?  └── logging_handler.py      # �?新增: 结构化日�?TraceId注入
�?├── monitor/                    # �?新增
�?  ├── dashboard.py            # FastAPI 实时面板
�?  ├── pnl_tracker.py          # P&L 追踪 (Sharpe/Sortino/MaxDD)
�?  ├── performance_tracker.py  # 性能统计
�?  └── system_health.py        # 系统健康检�?�?├── orchestrator/
�?  ├── main_loop.py            # 主循环编�?�?  └── alerting.py             # 告警通知
�?├── execution/
�?  ├── channels/order_sender.py
�?  ├── risk/risk_manager.py
�?  └── settlement/pnl_aggregator.py
�?├── scripts/                    # �?新增
�?  ├── train_alphacast.py      # AlphaCast 训练管线 (Focal Loss+早停+导出)
�?  └── gen_lgbm_onnx.py        # LightGBM→ONNX 生成�?�?├── data/shm_bridge.py          # Python SHM 零拷贝桥�?├── common/                     # 公共�?�?  ├── config.py               # 配置管理
�?  ├── engine.py               # 引擎基类
�?  └── logging_setup.py        # JSON 结构化日�?�?├── triton_model_repository/    # Triton 模型仓库
�?  ├── alphacast_resnet/config.pbtxt
�?  ├── lgbm_proxy/config.pbtxt
�?  └── resnet_encoder/config.pbtxt
�?├── docker-compose.yml          # Redis + TimescaleDB + Triton
├── pyproject.toml              # Python 依赖
├── v8_core_engine.pyi          # Rust 类型存根
└── tests/                      # 测试套件
```

---

## 3. 模块依赖关系

### 3.1 核心链路

```
pipeline_v2.py
  ├── harness/logging_handler.py  (日志注入)
  ├── alpha/crypto/obi_v2.py      (OBI信号)
  ├── alpha/crypto/funding_rate_arb.py (资金费率)
  ├── features/feature_fusion.py  (178d组装)
  �?    └── data/shm_bridge.py �?core/src/buffer.rs (SHM)
  ├── models/alphacast/alphacast_recalib.py (二次校准)
  �?    └── models/alphacast/serve_alphacast.py �?Triton
  └── models/mcts/mcts_worker.py  (规划)
        ├── [native] core/src/mcts_core.rs �?MctsPool
        └── [fallback] models/mcts/ev_threshold.py
```

### 3.2 训练链路

```
scripts/train_alphacast.py
  ├── labeling/counterfactual_labeler.py (标签生成)
  �?    └── TimescaleDB / Parquet (数据�?
  ├── features/feature_fusion.py (特征组装)
  └── models/alphacast/alphacast_model.py (模型定义)
        └── Triton 导出 (.pt / .onnx)
```

### 3.3 在线微调链路

```
calibration/alphacast_online_update.py
  ├── models/alphacast/alphacast_model.py (冻结body, 微调head)
  ├── calibration/temperature_scaling.py (Temperature Scaling)
  └── triton_model_repository/ (hot-reload)
```

---

## 4. Phase 完成�?(二次审计修正�?

| Phase | 交付�?| 完成 | 完成�?| 关键缺口 |
|-------|--------|------|--------|----------|
| P0 基建与契�?| 5 | 4 | 80% | —|
| P1 MVP 闭环 | 7 | 6.5 | 93% | logging_handler结构 |
| P2 ResNet+标签 | 6 | 5 | 83% | feature_fusion.py在features/ (非models/resnet/) |
| P3 AlphaCast+Triton | 7 | 5.5 | 79% | lgbm_proxy.onnx占位(可生�?, train在scripts/ |
| P4 Tokio MCTS | 5 | 5 | **100%** �?| 全部完成 |
| P5 微调+实盘 | 5 | 3 | 60% | 缺load_test, settings.py |
| **总计** | **35** | **~29** | **~83%** | |

### 实际路径 vs 蓝图期望

| 蓝图期望 | 实际位置 | 状�?|
|----------|----------|------|
| models/resnet/feature_fusion.py | features/feature_fusion.py | �?路径偏差 |
| models/mcts/ev_threshold.py | models/mcts/ev_threshold.py | �?|
| models/mcts/mcts_worker.py | models/mcts/mcts_worker.py | �?|
| labeling/counterfactual_labeler.py | labeling/counterfactual_labeler.py | �?|
| calibration/alphacast_online_update.py | calibration/ | �?|
| monitor/dashboard.py | monitor/ | �?|
| monitor/pnl_tracker.py | monitor/ | �?|
| scripts/train_alphacast.py | scripts/train_alphacast.py | �?|
| harness/logging_handler.py | harness/logging_handler.py | �?|
| harness/pipeline_v2.py | harness/pipeline_v2.py | �?|

---

## 5. 开发工作流

### 5.1 本地开�?(Windows/Firebat)

```powershell
# Python 环境
cd C:\Users\jerry\Desktop\V8
uv sync  # �?pip install -e .

# Rust 编译 (需 MSVC + Rust toolchain)
cd core
cargo check
cargo build --release
maturin develop --release  # 生成 .pyd 本地导入

# 基础设施
docker-compose up -d redis timescaledb  # Triton 需GPU

# 监控面板
python -m monitor.dashboard --port 8080
# 打开 http://localhost:8080

# 运行测试
pytest tests/ -v
```

### 5.2 N150 部署 (Linux x64)

```bash
# 1. 同步代码
rsync -avz --exclude '__pycache__' --exclude 'target' \
  V8/ jerry@192.168.1.103:/home/jerry/qts_v8/

# 2. 编译 Rust (已有环境)
cd /home/jerry/qts_v8
cargo build --release
maturin build --release

# 3. 安装 wheel
python3.11 -m pip install --user --force-reinstall \
  target/wheels/qts_v8-0.1.0-cp310-abi3-manylinux_2_34_x86_64.whl

# 4. 验证
python3.11 -c "
from v8_core_engine import ShmBridge, FeatureEngine, OrderFSM, MctsPool
b = ShmBridge()
f = FeatureEngine()
print('OK')
"

# 5. 运行主循�?python3.11 orchestrator/main_loop.py
```

### 5.3 Cargo.toml 关键配置

```toml
[dependencies]
pyo3 = { version = "0.21", features = ["extension-module", "abi3-py310"] }
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.11", default-features = false, features = ["json", "rustls-tls"] }
redis = { version = "0.25", default-features = false, features = ["tokio-comp"] }
prost = "0.12"
ring = "0.17"
memmap2 = "0.9"
```

**重要**: Rust 编译依赖 `crates.io` USTC 镜像 (`sparse+https://mirrors.ustc.edu.cn/crates.io-index/`)，N150 已配置�?
---

## 6. 配置指南

### 6.1 api_keys.yaml

```yaml
okx:
  demo:
    api_key: "your_demo_key"
    secret_key: "your_demo_secret"
    passphrase: "your_passphrase"
    base_url: "https://www.okx.com"
    demo: true
```

### 6.2 v8.yaml

```yaml
inst_id: "BTC-USDT-SWAP"
mode: "demo"  # demo | live
mcts:
  backend: "native"  # native | fallback
  timeout_ms: 100
  num_workers: 8
alpha:
  obi_threshold: 0.6
  funding_rate_arb: true
gating:
  g1_min_depth: 50000  # $50K
  g2_max_hv: 0.04      # 4%
  g5_enabled: true     # 结算�?0min禁开新仓
calibration:
  online_update_enabled: false  # P5阶段启用
  temperature_update_interval: 50
```

### 6.3 环境变量

```bash
V8_DB_PASSWORD=v8_quant_2026     # TimescaleDB密码
REDIS_URL=redis://localhost:6379  # Redis地址
V8_LOG_DIR=logs/                  # 日志目录
V8_MODE=demo                      # demo | live
```

---

## 7. 测试

### 7.1 自测覆盖

每个新模块均包含 `if __name__ == "__main__"` 自测块：

```bash
python alpha/crypto/obi_v2.py
python features/feature_fusion.py
python labeling/counterfactual_labeler.py
python models/mcts/ev_threshold.py
python models/mcts/mcts_worker.py
python harness/pipeline_v2.py
python calibration/temperature_scaling.py
python calibration/alphacast_online_update.py
```

### 7.2 集成测试

```bash
pytest tests/ -v --tb=short
# 现有测试: test_config, test_market_data, test_okx_rest,
#           test_orderbook, test_risk_manager
```

### 7.3 压测 (待实�?

```bash
# P5 交付�?(tests/load_test.py)
python tests/load_test.py --tps 500 --duration 4h
```

---

## 8. 运维手册

### 8.1 监控面板

```bash
uvicorn monitor.dashboard:app --host 0.0.0.0 --port 8080
```

端点:
- `GET /api/status` —系统健康状�?- `GET /api/pnl` —P&L 汇�?- `GET /api/metrics` —Prometheus 格式
- `WS /ws` —实时推�?
### 8.2 日志

使用 `harness/logging_handler.py` 的生产环境日志配置：

```python
from harness.logging_handler import setup_production_logging
setup_production_logging(log_dir="logs/", level="INFO", enable_file=True)
```

格式: `{"ts": "2026-05-31T14:00:00Z", "level": "INFO", "msg": "pulse_v2_done", "trace_id": "xxx", "action": "buy", ...}`

### 8.3 告警

```python
from orchestrator.alerting import send_alert
send_alert("G1 block rate > 80%", level="warning")
```

通道: Telegram / 钉钉 (可配�?

### 8.4 降级切换

```python
# MCTS 降级
from models.mcts.mcts_worker import MctsWorker
worker = MctsWorker(force_fallback=True)

# Calibration 暂停
from calibration.alphacast_online_update import AlphaCastOnlineUpdater
updater._is_paused = True
```

---

## 9. FAQ

**Q: 为什�?feature_fusion.py �?features/ 而非 models/resnet/?**
A: 因为它是特征组装工具（不依赖PyTorch），属于特征工程层，不应放在模型目录。蓝图建议的位置可能不太合适——保持现状�?
**Q: lgbm_proxy.onnx 为什么是 placeholder?**
A: LightGBM 替身模型需离线训练生成。运�?`python scripts/gen_lgbm_onnx.py --data cfl_labels.parquet --output triton_model_repository/lgbm_proxy/1/lgbm_proxy.onnx` 生成真实模型�?
**Q: N150 编译遇到 libssl 问题怎么解决?**
A: Cargo.toml 已禁�?native-tls，全部使�?rustls。如仍报错，检�?`~/.cargo/config.toml` 是否指向 USTC 镜像�?
**Q: PipelineV2 �?AlphaCast 调用走了替身?**
A: 目前 PipelineV2 �?`fused_signal * 0.01` 作为 predicted_return 代理。Phase 3 正式版中替换�?Triton gRPC 调用 `serve_alphacast.py`�?
---

## 10. 下一步清�?
| 优先�?| 任务 | Phase | 预计工时 |
|--------|------|-------|----------|
| P0 | tests/load_test.py (500TPS压测) | P5 | 2h |
| P0 | config/settings.py (LIVE_MODE切换) | P5 | 1h |
| P1 | lgbm_proxy.onnx 真实生成 | P3 | 3h (需历史数据) |
| P2 | Triton Docker 环境验证 | P3 | 2h |
| P2 | 端到�?Demo 环境集成测试 | P1 | 4h |
| P3 | N150 完整部署 (docker + 代码) | P1 | 2h |