# V8 项目归档整理 2026-05-31

## 目标
对 V8 项目目录进行归档整理，清理无用文件，补全蓝图缺口。

## 清理项目

### 删除 (43 项)
| 类型 | 数量 | 详情 |
|------|------|------|
| 废弃临时脚本 | 5 | `_deploy1.py, _pack.py, _probe.py, _smoke_n150.py, _ssh_test.py` |
| 孤立文档归档 | 3 | → `doc/archive/` (code-documentation-complete, v8_directory_setup, v8_fix_audit) |
| 孤儿 conftest.py | 1 | 根目录，`tests/conftest.py` 是正式版 |
| 空目录 | 11 | `alpha/cross_market, equity, forex, poly` + `infra/deploy, proxy, redis` + `risk` + `models/mcts/src` + `data/sources` + `execution_rust` + `tests/integration, unit` |
| __pycache__ | 22 | 全部 .pyc 缓存目录 + 1 根目录孤儿 |
| infra/ 空包 | 1 | 子目录全部空，整体删除 |
| python/ 无引用 | 1 | 仅含 `__init__.py`，无任何模块引用 |

### 保留 (未删除)
| 项目 | 原因 |
|------|------|
| `adapters/okx_rest.py` + `okx_ws.py` | 被 `data/market_data.py`, `tests/conftest.py`, `tests/test_okx_rest.py` 引用 |
| `models/resnet/feature_fusion.py` | 非重复 — 实现 50d⊕128d→100d，为 `features/feature_fusion.py` 提供 DIM_RESNET=100 |
| `features/feature_fusion.py` | 非重复 — 实现 50d+12d+16d+100d→178d 全栈融合 |
| `ml/meta_labeler.py` | 被 `ml/__init__.py` 正式导入 |
| `Cargo.toml` (根目录) | 工作区根 Cargo 配置 (core/ 子目录编译依赖此文件) |

### 新增
- `config/settings.py` (7.8KB) — P5 最后缺口，demo/live 环境切换 + YAML 加载 + 环境变量覆盖

## 最终状态
- 35 目录, 109 文件, 1140KB (不含 .git)
- 蓝图完成度: **~94% (33/35)**
- 唯一缺口: `lgbm_proxy.onnx` 真实模型 → 运行 `scripts/gen_lgbm_onnx.py` 生成

## 产出
- `config/settings.py` — 部署环境配置
- `doc/archive/` — 历史文档归档